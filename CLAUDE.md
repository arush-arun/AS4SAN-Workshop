# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This repository will hold a **20-minute workshop talk** on reproducible data management for big neuroimaging, built around **DataLad**. The specification lives in `claude-code-prompt-datalad-talk.md`.

## Tech Stack

- **Slides:** Quarto → reveal.js (`.qmd` source, self-contained HTML + PDF output)
- **Demo:** Shell script recorded via asciinema (pre-recorded casts, never live git-annex)
- **Example dataset:** Tiny DataLad dataset creation script, guarded for missing `datalad`

## Key Constraints

- Rendering must work **without** `datalad` installed — all datalad output is pre-captured static text/casts
- Do **not** silently install system packages; report what's missing with install instructions
- Slide content is placeholder-only (`TODO` markers) — prose is drafted separately
- The repo itself should be version-controlled with small, logically-scoped commits

## Build Commands (once scaffolded)

```bash
quarto render slides.qmd --to html    # self-contained HTML deck
quarto render slides.qmd --to typst --output slides.pdf  # PDF fallback (typst engine)
```

## Target Structure

See `claude-code-prompt-datalad-talk.md` for the full specification including repo layout, slide beat list, demo script requirements, and example dataset requirements.


<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

*No recent activity*
</claude-mem-context>